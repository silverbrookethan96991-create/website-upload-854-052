function bootPlayer(videoId,coverId,src){var video=document.getElementById(videoId),cover=document.getElementById(coverId);if(!video||!cover)return;var hls=null,started=false;function start(){if(started){video.play();return}started=true;cover.classList.add('hide');if(video.canPlayType('application/vnd.apple.mpegurl')){video.src=src;video.play().catch(function(){})}else if(window.Hls&&window.Hls.isSupported()){hls=new Hls({enableWorker:true,lowLatencyMode:true});hls.loadSource(src);hls.attachMedia(video);hls.on(Hls.Events.MANIFEST_PARSED,function(){video.play().catch(function(){})})}else{video.src=src;video.play().catch(function(){})}}cover.addEventListener('click',start);video.addEventListener('click',function(){if(!started){start();return}if(video.paused){video.play().catch(function(){})}else{video.pause()}});window.addEventListener('pagehide',function(){if(hls){hls.destroy()}})}